source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

platform :ios, '9.3'
inhibit_all_warnings!

project 'Notify'
use_frameworks!

target 'Notify_Example' do
  pod 'Notify', :path => './'
end

target 'Notify_Tests' do
  pod 'Notify', :path => './'

  pod 'Quick'
  pod 'Nimble'
end
